#include <iostream>
using namespace std ;
int main ()
{
    float length,breadth,area;
    cout<<"enter the length and breadth"<<endl;
    cin>>length>>breadth;
    area=1.0/2.0*length*breadth;
    cout<<"area of the trangle is "<< area;
    return 0;
}
