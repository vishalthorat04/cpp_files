//to take the radius and calculate the area of the circle
#include <iostream>
using namespace std;
int main()
{
    float radius,area;
    cout<<"Enter the radius of the circle ";
    cin>>radius;
    area=3.14*radius*radius;
    cout<<"area of the circle is "<<area;
    return 0;
}
